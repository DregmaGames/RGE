import { en } from './languages/en';
import { es } from './languages/es';
import { it } from './languages/it';
import { de } from './languages/de';

export type Language = 'en' | 'es' | 'it' | 'de';

export const translations = {
  en,
  es,
  it,
  de
};